/// <reference path="utility-function.ts" />

const result1 = Utility.maxBooksAllowed(10);
console.log(result1);

import util = Utility.Fees;
const result2 = util.calculateLateFree(10);
console.log(result2);
